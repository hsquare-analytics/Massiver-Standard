spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop  # 애플리케이션 실행 시 테이블 자동 업데이트
    show-sql: true  # SQL 쿼리 로깅 활성화
  batch:
    jdbc:
      initialize-schema: always  # 배치 스키마 초기화
    job:
      enabled: false  # 배치 잡 비활성화
